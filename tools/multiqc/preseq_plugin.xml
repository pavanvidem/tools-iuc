<macros>
    <token name="@PRESEQ_COMMAND@"><![CDATA[
        #for $file in $repeat.software_cond.input:
            @ESCAPE_IDENTIFIER@
            #set safe_id = str($identifier).replace(" ", "_")
            #set file_path = os.path.join($software_dir, $safe_id + '.ccurve.txt')
            ln -s '$file' '$file_path' &&
        #end for
    ]]></token>
    
    <xml name="preseq_form">
        <param name="input" type="data" format="tabular,txt,tsv" multiple="true" label="Output of Preseq"/>
    </xml>
	
    <xml name="preseq_test">
        <test expect_num_outputs="3">
            <repeat name="results">
                <conditional name="software_cond">
                    <param name="software" value="preseq"/>
                    <param name="input" value="preseq.tsv"/>
                </conditional>
            </repeat>
            <param name="title" value="Test Report"/>
            <param name="export" value="true"/>
            <output name="html_report">
                <assert_contents>
                    <has_text text="Preseq"/>
                </assert_contents>
            </output>
        </test>
    </xml>
</macros>